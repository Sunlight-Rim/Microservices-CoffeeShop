syntax = "proto3";

package orders;

import "google/protobuf/timestamp.proto";

option go_package = ".;orders_pb";


service OrdersService {
    rpc Create (CreateOrderRequest) returns (CreateOrderResponse);
    rpc Get (GetOrderRequest) returns (GetOrderResponse); // one or more
    rpc Update (UpdateOrderRequest) returns (UpdateOrderResponse);
    rpc Delete (DeleteOrderRequest) returns (DeleteOrderResponse);
}

message Coffee {
    string type = 1;
    float price = 2;
}

message Order {
    enum Status {
        PENDING = 0;
        DELIVERED = 1;
    }
    int64 id = 1;
    repeated Coffee coffees = 2;
    float total = 3;
    google.protobuf.Timestamp date = 4;
    Status status = 5;
}

// Create

message CreateOrderRequest {
    string username = 1;
    string token = 2;
    repeated Coffee coffees = 3;
}

message CreateOrderResponse {
    Order order = 1;
}

// Get

message GetOrderRequest {
    string username = 1;
    string token = 2;
    repeated int64 ids = 3;
}

message GetOrderResponse {
    repeated Order orders = 1;
}

// Update

message UpdateOrderRequest {
    string username = 1;
    string token = 2;
    int64 id = 3;
    repeated Coffee coffees = 4;
}

message UpdateOrderResponse {
    Order order = 1;
}

// Delete

message DeleteOrderRequest {
    string username = 1;
    string token = 2;
    int64 id = 3;
}

message DeleteOrderResponse {
    Order order = 1;
}